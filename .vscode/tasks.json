{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build",
            "type": "shell",
            "command": "gcloud functions deploy tarot-bot --trigger-http --region=europe-west6 --memory=256Mb --runtime=python310 --entry-point=main"
        }
    ]
}
